PORT=3001
GROQ_API_KEY=your_groq_api_key_here
CORS_ORIGIN=http://localhost:3000
# For multiple origins, use comma-separated list:
# CORS_ORIGIN=http://localhost:3000,https://example.com,https://app.example.com

# Keep-alive settings (for Render free tier to prevent spin-down)
# Enable self-pinging keep-alive mechanism (default: true)
ENABLE_KEEP_ALIVE=true
# Keep-alive ping interval in milliseconds (default: 600000 = 10 minutes)
KEEP_ALIVE_INTERVAL=600000
# Custom URL for keep-alive (default: http://localhost:PORT/health)
# On Render, set this to your deployed URL or keep-alive won't prevent spin-down:
KEEP_ALIVE_URL=https://your-app.onrender.com/health
